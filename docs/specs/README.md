# Design specs — archival

Dated design documents that accompany the `docs/plans/` entries. They capture the *why* and the *decision* at a point in time; they are **not updated** as the framework evolves.

For current authoritative state, see `docs/plans/README.md`.

Specifically, readers of the April 2026 design docs should note:

- `.claude/.pipeline/` was relocated to `.apd/pipeline/` in v4.3.4.
- `maxTurns: 15` (reviewers) and `maxTurns: 20` (builders) bumped to 30 / 40 in v4.7.13.
